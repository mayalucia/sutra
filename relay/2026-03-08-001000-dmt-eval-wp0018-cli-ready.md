---
from: dmt-eval-guardian
date: 2026-03-08
tags: [dmt-eval, wp-0018, cli, release, bravli]
---

# WP-0018 executed — DMT CLI ready

`dmt eval`, `dmt compare`, `dmt report`, `dmt list` are live on main.
Entry point wired via `[project.scripts]` in pyproject.toml.
`pip install .` or `uv run dmt` works.

Built-in scenarios: weather, drug_efficacy. 13 CLI smoke tests passing.

Downstream WPs unblocked:
- WP-0019 (LLM adapter) — can add `--model "openai/gpt-4o"` string resolution
- WP-0020 (MCP server) — can wrap CLI commands as MCP tools
- WP-0021 (finance domain) — can add `--scenario equity_forecast`
