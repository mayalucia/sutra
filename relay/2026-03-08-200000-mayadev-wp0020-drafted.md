---
from: mayadev/claude-opus-4-6@vadda
date: 2026-03-08T20:00:00Z
tags: [wp-0020, dp-0003, story-browser, sutradhar, drafted]
---

## WP-0020 drafted — Story Browser Shell (Phase 1)

The missing WP in the story browser chain (DP-0003) now exists as
`workpacks/0020-browser-shell.org`.

### What it is

Fork the constellation viewer (`website/project-constellation/`) into
a two-view story browser at `website/story-browser/`:

1. **Geography view** — schematic SVG map of Western Himalaya with
   story pins at correct relative positions. Tirthan cluster separable.
   Karakoram empty.
2. **Concept view** — force-directed graph of 93 translation keys.
   Stories as connecting elements between concepts.
3. **Info panel** — shared between views. Story metadata, illustration
   thumbnails, translation keys, link to full story.
4. **View switching** — toggle between geography and concept projections.

### Dependencies (both satisfied)

- WP-0015 (stories.edn) — ✓ completed. 12 stories, 93 concepts,
  18 characters, typed edges.
- WP-0016 (ecological web) — ✓ completed.

### For sutradhar-guardian

You are the executor. The constellation viewer source at
`website/project-constellation/` is your starting point — 10 CLJS
files, ~1,400 lines. Fork the force simulation, state management,
and edge rendering. Write geography view and info panel fresh.

The EDN data is at `workpacks/data/stories.edn`. Place it into
`website/story-browser/src/story_browser/data.cljs` (or load at
build time).

Output JS to `website/static/js/story-browser.js`. Hugo page at
`website/content/writing/browser.md`.

### Chain status

```
WP-0015 ✓ ──────┐
                 ├──► WP-0020 ← YOU ARE HERE
WP-0016 ✓ ──────┘       │
                         ▼
                   WP-0014 final
                    (human work)
```
